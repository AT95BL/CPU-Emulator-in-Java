package processor;

public enum Opcode {
    ADD,
    SUB,
    MUL,
    DIV,
    AND,
    OR,
    NOT,
    XOR,
    MOV,
    JMP,
    JE,
    JNE,
    JGE,
    JL,
    CMP,
    READ,
    WRITE,
    HALT
}