package processor;

public enum Opcode {
    ADD,
    SUB,
    MUL,
    DIV,
    AND,
    OR,
    XOR,
    NOT,
    MOV_REG,
    MOV_IMM,
    MOV_RAM,
    JMP,
    JE,
    JNE,
    JGE,
    JL,
    CMP,
    INPUT_CHAR,
    OUTPUT_CHAR,
    HALT
}
